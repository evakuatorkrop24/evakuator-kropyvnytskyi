
1. Header nav elements — for example `<a class="nav-link text-white" href="#services">Послуги</a>` — are white by default and are not visible on a white background.
2. On the Terms and Privacy pages, the call button is still large on mobile but should be smaller, like on the landing page.
3. It may be a good idea to make the header sticky at the top, especially on mobile (or make it non-transparent). I need a recommendation.
4. In the Reviews and Gallery sections, `.swiper-button-next` and `.swiper-button-prev` are displayed as text on the Cloudflare environment; locally it is fine.
5. Seed/stub images are not displayed on the Cloudflare environment; locally it is fine.